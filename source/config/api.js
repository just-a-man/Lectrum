// Core
import { getFullApiUrl } from 'instruments';

const GROUP_ID = 'ymuzjtt5jele';
const TOKEN = 'su6omu52b9';
const url = 'https://lab.lectrum.io/react/api';
const api = getFullApiUrl(url, GROUP_ID);

export { GROUP_ID, TOKEN, api, url };
